<%= form_for @micropost  , :html => { :multipart => true } do |f| %>

	

	
	<div class="field">
		<%= f.text_area :content %>
		<%= f.file_field :photo %>
	</div>
	<div class="actions">
	
	<%= f.submit "Say Something" %>
	
	</div>
	


<% end %>